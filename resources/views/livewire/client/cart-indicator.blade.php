<a x-data="{ pulse: false, timer: null }" x-on:cart-feedback.window="if ($event.detail.added) { pulse = false; clearTimeout(timer); $nextTick(() => { pulse = true; timer = setTimeout(() => pulse = false, 700) }) }" :class="{ 'cart-pulse': pulse }" href="{{ route('cart') }}" class="header-cart" aria-label="Giỏ hàng, {{ $count }} sản phẩm"><i class="bi bi-bag"></i><span>Giỏ hàng</span>@if($count > 0)<b class="cart-count" aria-live="polite">{{ $count }}</b>@endif</a>
